const path = (root, sublink) => {
  return `${root}${sublink}`;
};

const ROOTS_PAGE = "/";
const ROOTS_AUTH = "/auth";

const PATH_AUTH = {};
const PATH_PAGE = {};

export default { PATH_PAGE, PATH_AUTH };
